// function getDigitalRoot(num) {
//   return `${num}`.split('').map(Number).reduce((total, item) => total += item, 0)
//     .toString().split('').map(Number).reduce((total, item) => total += item, 0);
// }

// console.log(getDigitalRoot(23456));
